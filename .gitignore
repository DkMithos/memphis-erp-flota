# Dependencias
node_modules/

# Build outputs
dist/
build/

# Variables de entorno (nunca al repositorio)
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
*.env

# Archivos temporales de PR / AI (no deben estar en el repo)
pr_*.json
pr_*.md
temp_*

# Scripts de carga histórica (one-off, no versionar)
scripts/imports-historicos/

# Backups de datos (dumps locales pre-migración, nunca al repo)
backups/

# Claude Code (contiene tokens y configuración local)
.claude/settings.local.json
.claude/worktrees/
.claude/skills/
.claude/agents/
.claude/superpowers/

# Skills CLI (tooling local, no versionar)
.agents/
skills-lock.json

# Editor
.vscode/settings.json
.vscode/launch.json
*.suo
*.ntvs*
*.njsproj
*.sln

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Vercel
.vercel/.env.*

# TypeScript
*.tsbuildinfo

# Lockfile excluded - platform-specific deps cause cross-platform issues
package-lock.json
scripts/migration-oc/
